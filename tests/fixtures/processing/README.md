# Processing fixtures

| Path | Role |
|---|---|
| `golden/` | **DOC-14** inventory (`catalog.json`) + extra digital/scanned/Excel goldens |
| `vietnamese_sample.pdf` | DOC-03 digital Vietnamese text |
| `tables/` | DOC-11 synthetic table PDFs + expected matrices |
| `tables/fpt_raw/` | DOC-12 vetted FPT raw-table JSON (reconstruction goldens) |
| `tables/fpt_real/` | Local-only issuer PDF exports (gitignored binaries + committed manifest) |

See `golden/README.md` for DOC-14 targets and source-policy notes.
